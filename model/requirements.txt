# Core — required for all stages
opencv-python>=4.8
ultralytics>=8.0
numpy>=1.24
python-dotenv>=1.0

# Stage 3 — face recognition
insightface>=0.7
onnxruntime>=1.16

# Stage 5 — vision LLM narration
# Azure OpenAI GPT-4o (primary, per the activity-tracker plan):
openai>=1.0

# Google Gemini (alternative fallback):
google-generativeai>=0.5
